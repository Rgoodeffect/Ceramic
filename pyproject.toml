[project]
name = "retail_suite"
authors = [
    { name = "Retail Suite for ERPNext", email = "support@retailsuite.example" }
]
description = "Multi-vertical retail platform for ERPNext. First vertical: Ceramic Showroom."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
